import { EndDirective } from './end.directive';

describe('EndDirective', () => {
  it('should create an instance', () => {
    const directive = new EndDirective();
    expect(directive).toBeTruthy();
  });
});
